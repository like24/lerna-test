import { Core } from '@example/core';
export declare function createGreeting(name: string): string;
export declare function formatMessage(core: Core, message: string): string;
export declare function createMultiCore(names: string[]): Core[];
export declare const UTILS_VERSION = "0.0.0";
